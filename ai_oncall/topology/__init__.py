"""Stage 2 (ASSEMBLE) topology component. Builds a tenant-scoped service graph
from observed spans with a 10-minute decay window; falls back to topology.yaml.
Implementation lands in BRIEF.md step 4."""
